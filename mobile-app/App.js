// Simple App.js entry point for web
import { registerRootComponent } from 'expo';
import Main from './App.tsx';

// Register the main App component as the root component
registerRootComponent(Main);

export default Main;