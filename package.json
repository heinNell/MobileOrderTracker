{
  "name": "mobile-order-tracker-android",
  "version": "1.0.0",
  "scripts": {
    "expo:start": "cd .. && npx expo start --clear",
    "expo:android": "cd .. && npx expo run:android --no-build-cache",
    "clean": "rm -rf .gradle app/build ../.expo"
  }
}
